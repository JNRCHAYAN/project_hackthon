# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
*.egg-info/

# Runtime artifacts (regenerable — not source)
data/audit.sqlite
data/narrations/*.json
data/metrics.json

# Secrets — never commit
.env
.env.local

# Unrelated local tool backup present in this folder. Must not be published;
# it contains another project's session export and configuration.
.kilo/

# OS / editor
.DS_Store
Thumbs.db
.idea/
